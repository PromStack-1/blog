---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 6);
---

<!doctype html>
<html lang="ko">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
	</head>
	<body>
		<Header />
		
		<!-- Animated Background -->
		<div class="bg-glow glow-1"></div>
		<div class="bg-glow glow-2"></div>
		<div class="bg-glow glow-3"></div>
		
		<main>
			<!-- Hero Section - Creative Layout -->
			<section class="hero">
				<div class="hero-container">
					<div class="hero-badge">
						<span class="badge-dot"></span>
						<span>AI-First Development</span>
					</div>
					
					<h1 class="hero-title">
						<span class="line">Where</span>
						<span class="line highlight">Intelligence</span>
						<span class="line">Meets Craft</span>
					</h1>
					
					<p class="hero-desc">
						AI 시대의 개발자를 위한 블로그. PromStack과 LinkStack을 만들며<br/>
						배운 것들을 나눕니다.
					</p>
					
					<div class="hero-stats">
						<div class="stat">
							<span class="stat-number" id="counter-1">12</span>
							<span class="stat-label">Articles</span>
						</div>
						<div class="stat-divider"></div>
						<div class="stat">
							<span class="stat-number">2</span>
							<span class="stat-label">Products</span>
						</div>
						<div class="stat-divider"></div>
						<div class="stat">
							<span class="stat-number">∞</span>
							<span class="stat-label">Possibilities</span>
						</div>
					</div>
					
					<div class="hero-cta">
						<a href="/blog" class="cta-primary">
							<span>Start Reading</span>
							<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M5 12h14M12 5l7 7-7 7"/>
							</svg>
						</a>
						<a href="https://promstack.com" target="_blank" class="cta-secondary">
							Try PromStack
						</a>
					</div>
				</div>
				
				<!-- Floating Cards -->
				<div class="floating-cards">
					<div class="float-card card-1">
						<span class="card-icon">📝</span>
						<span class="card-text">Prompt Engineering</span>
					</div>
					<div class="float-card card-2">
						<span class="card-icon">🔗</span>
						<span class="card-text">Knowledge Curation</span>
					</div>
					<div class="float-card card-3">
						<span class="card-icon">⚡</span>
						<span class="card-text">AI Workflow</span>
					</div>
				</div>
			</section>

			<!-- Products Showcase -->
			<section class="products">
				<div class="products-header">
					<span class="section-eyebrow">Ecosystem</span>
					<h2 class="section-title">Tools We Build</h2>
				</div>
				
				<div class="products-showcase">
					<a href="https://promstack.com" target="_blank" class="product-large">
						<div class="product-glow"></div>
						<div class="product-content">
							<div class="product-badge">Featured</div>
							<div class="product-icon-large">📝</div>
							<h3>PromStack</h3>
							<p>AI 프롬프트 관리 플랫폼. 팀 협업, 버전 관리, MCP 통합을 지원하는 올인원 솔루션.</p>
							<span class="product-cta">promstack.com →</span>
						</div>
						<div class="product-visual">
							<div class="visual-box"></div>
							<div class="visual-box"></div>
							<div class="visual-box"></div>
						</div>
					</a>
					
					<div class="products-side">
						<a href="https://link.promstack.com" target="_blank" class="product-small">
							<span class="product-icon-small">🔗</span>
							<div>
								<h4>LinkStack</h4>
								<p>AI 기반 북마크 & 하이라이트</p>
							</div>
							<span class="arrow">→</span>
						</a>
						<a href="https://github.com/PromStack-1" target="_blank" class="product-small">
							<span class="product-icon-small">💻</span>
							<div>
								<h4>Open Source</h4>
								<p>SDK & Libraries</p>
							</div>
							<span class="arrow">→</span>
						</a>
					</div>
				</div>
			</section>

			<!-- Latest Posts -->
			<section class="posts-section">
				<div class="posts-header">
					<div>
						<span class="section-eyebrow">Latest</span>
						<h2 class="section-title">Fresh Insights</h2>
					</div>
					<a href="/blog" class="view-all">All posts →</a>
				</div>
				
				<div class="posts-grid">
					{posts.slice(0, 1).map((post) => (
						<a href={`/blog/${post.id}/`} class="post-featured">
							<div class="post-image-featured">
								{post.data.heroImage && (
									<img src={typeof post.data.heroImage === 'string' ? post.data.heroImage : post.data.heroImage.src} alt="" />
								)}
							</div>
							<div class="post-content-featured">
								<span class="post-tag">Featured</span>
								<h3>{post.data.title}</h3>
								<p>{post.data.description}</p>
								<span class="post-date"><FormattedDate date={post.data.pubDate} /></span>
							</div>
						</a>
					))}
					
					<div class="posts-list">
						{posts.slice(1, 4).map((post, i) => (
							<a href={`/blog/${post.id}/`} class="post-item">
								<span class="post-number">0{i + 2}</span>
								<div class="post-info">
									<h4>{post.data.title}</h4>
									<span class="post-date"><FormattedDate date={post.data.pubDate} /></span>
								</div>
								<span class="post-arrow">→</span>
							</a>
						))}
					</div>
				</div>
			</section>
		</main>
		
		<Footer />
	</body>
</html>

<style is:global>
	:root {
		--bg: #050505;
		--bg-elevated: #0a0a0a;
		--bg-card: #111111;
		--text: #ffffff;
		--text-secondary: #888888;
		--text-muted: #555555;
		--accent: #6366f1;
		--accent-secondary: #8b5cf6;
		--border: rgba(255, 255, 255, 0.08);
	}

	* { margin: 0; padding: 0; box-sizing: border-box; }

	body {
		background: var(--bg);
		color: var(--text);
		font-family: 'Inter', -apple-system, sans-serif;
		overflow-x: hidden;
		position: relative;
	}

	/* Animated Background Glows */
	.bg-glow {
		position: fixed;
		border-radius: 50%;
		filter: blur(120px);
		opacity: 0.4;
		pointer-events: none;
		z-index: 0;
	}
	.glow-1 {
		width: 600px; height: 600px;
		background: linear-gradient(135deg, #6366f1, #8b5cf6);
		top: -200px; left: -200px;
		animation: float1 20s ease-in-out infinite;
	}
	.glow-2 {
		width: 500px; height: 500px;
		background: linear-gradient(135deg, #ec4899, #8b5cf6);
		top: 50%; right: -150px;
		animation: float2 25s ease-in-out infinite;
	}
	.glow-3 {
		width: 400px; height: 400px;
		background: linear-gradient(135deg, #06b6d4, #6366f1);
		bottom: -100px; left: 30%;
		animation: float3 18s ease-in-out infinite;
	}
	@keyframes float1 {
		0%, 100% { transform: translate(0, 0) scale(1); }
		50% { transform: translate(50px, 50px) scale(1.1); }
	}
	@keyframes float2 {
		0%, 100% { transform: translate(0, 0) scale(1); }
		50% { transform: translate(-30px, -40px) scale(0.9); }
	}
	@keyframes float3 {
		0%, 100% { transform: translate(0, 0) scale(1); }
		50% { transform: translate(40px, -30px) scale(1.05); }
	}

	main {
		position: relative;
		z-index: 1;
		max-width: 1400px;
		margin: 0 auto;
		padding: 0 3rem;
	}

	/* Hero */
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 4rem;
		padding: 6rem 0;
		position: relative;
	}

	.hero-container {
		flex: 1;
		max-width: 700px;
	}

	.hero-badge {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.5rem 1rem;
		background: rgba(99, 102, 241, 0.1);
		border: 1px solid rgba(99, 102, 241, 0.2);
		border-radius: 100px;
		font-size: 0.75rem;
		font-weight: 600;
		color: var(--accent);
		margin-bottom: 2rem;
	}
	.badge-dot {
		width: 6px; height: 6px;
		background: var(--accent);
		border-radius: 50%;
		animation: pulse 2s ease-in-out infinite;
	}
	@keyframes pulse {
		0%, 100% { opacity: 1; }
		50% { opacity: 0.4; }
	}

	.hero-title {
		margin-bottom: 2rem;
	}
	.hero-title .line {
		display: block;
		font-size: clamp(3rem, 8vw, 5.5rem);
		font-weight: 900;
		line-height: 1;
		letter-spacing: -0.04em;
	}
	.hero-title .highlight {
		background: linear-gradient(135deg, var(--accent), var(--accent-secondary), #ec4899);
		background-size: 200% auto;
		-webkit-background-clip: text;
		background-clip: text;
		-webkit-text-fill-color: transparent;
		animation: gradient-shift 3s ease infinite;
	}
	@keyframes gradient-shift {
		0%, 100% { background-position: 0% center; }
		50% { background-position: 100% center; }
	}

	.hero-desc {
		font-size: 1.125rem;
		color: var(--text-secondary);
		line-height: 1.7;
		margin-bottom: 2.5rem;
	}

	.hero-stats {
		display: flex;
		align-items: center;
		gap: 2rem;
		margin-bottom: 3rem;
		padding: 1.5rem 0;
		border-top: 1px solid var(--border);
		border-bottom: 1px solid var(--border);
	}
	.stat { text-align: center; }
	.stat-number {
		display: block;
		font-size: 2rem;
		font-weight: 800;
		background: linear-gradient(135deg, var(--text), var(--text-secondary));
		-webkit-background-clip: text;
		background-clip: text;
		-webkit-text-fill-color: transparent;
	}
	.stat-label {
		font-size: 0.75rem;
		color: var(--text-muted);
		text-transform: uppercase;
		letter-spacing: 0.1em;
	}
	.stat-divider {
		width: 1px;
		height: 40px;
		background: var(--border);
	}

	.hero-cta {
		display: flex;
		gap: 1rem;
	}
	.cta-primary {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		padding: 1rem 1.75rem;
		background: linear-gradient(135deg, var(--accent), var(--accent-secondary));
		color: white;
		font-weight: 600;
		border-radius: 12px;
		text-decoration: none;
		transition: all 0.3s ease;
		box-shadow: 0 4px 20px rgba(99, 102, 241, 0.3);
	}
	.cta-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 30px rgba(99, 102, 241, 0.4);
	}
	.cta-secondary {
		padding: 1rem 1.75rem;
		color: var(--text);
		font-weight: 600;
		border: 1px solid var(--border);
		border-radius: 12px;
		text-decoration: none;
		transition: all 0.3s ease;
	}
	.cta-secondary:hover {
		background: rgba(255, 255, 255, 0.05);
		border-color: rgba(255, 255, 255, 0.15);
	}

	/* Floating Cards */
	.floating-cards {
		position: relative;
		width: 350px;
		height: 400px;
	}
	.float-card {
		position: absolute;
		display: flex;
		align-items: center;
		gap: 0.75rem;
		padding: 1rem 1.25rem;
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: 16px;
		backdrop-filter: blur(10px);
		box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
		animation: float 6s ease-in-out infinite;
	}
	.card-icon { font-size: 1.5rem; }
	.card-text { font-size: 0.875rem; font-weight: 600; }
	.card-1 { top: 0; left: 0; animation-delay: 0s; }
	.card-2 { top: 120px; right: 0; animation-delay: -2s; }
	.card-3 { bottom: 60px; left: 30px; animation-delay: -4s; }
	@keyframes float {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-15px); }
	}

	/* Products */
	.products {
		padding: 8rem 0;
	}
	.products-header, .posts-header {
		margin-bottom: 3rem;
	}
	.section-eyebrow {
		display: block;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.75rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.2em;
		color: var(--accent);
		margin-bottom: 0.5rem;
	}
	.section-title {
		font-size: clamp(2rem, 4vw, 3rem);
		font-weight: 800;
		letter-spacing: -0.02em;
	}

	.products-showcase {
		display: grid;
		grid-template-columns: 2fr 1fr;
		gap: 1.5rem;
	}
	.product-large {
		position: relative;
		padding: 3rem;
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: 24px;
		text-decoration: none;
		color: var(--text);
		overflow: hidden;
		transition: all 0.4s ease;
	}
	.product-large:hover {
		border-color: rgba(99, 102, 241, 0.3);
		transform: translateY(-4px);
	}
	.product-glow {
		position: absolute;
		top: -100px; right: -100px;
		width: 300px; height: 300px;
		background: radial-gradient(circle, rgba(99, 102, 241, 0.2), transparent);
		pointer-events: none;
	}
	.product-content { position: relative; z-index: 1; }
	.product-badge {
		display: inline-block;
		padding: 0.25rem 0.75rem;
		background: var(--accent);
		color: white;
		font-size: 0.6875rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		border-radius: 100px;
		margin-bottom: 1.5rem;
	}
	.product-icon-large {
		font-size: 3rem;
		margin-bottom: 1rem;
	}
	.product-content h3 {
		font-size: 2rem;
		font-weight: 700;
		margin-bottom: 0.75rem;
	}
	.product-content p {
		color: var(--text-secondary);
		line-height: 1.6;
		margin-bottom: 1.5rem;
		max-width: 400px;
	}
	.product-cta {
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.875rem;
		color: var(--accent);
	}
	.product-visual {
		position: absolute;
		bottom: 2rem; right: 2rem;
		display: flex;
		gap: 0.5rem;
	}
	.visual-box {
		width: 60px; height: 40px;
		background: rgba(99, 102, 241, 0.1);
		border: 1px solid rgba(99, 102, 241, 0.2);
		border-radius: 8px;
	}

	.products-side {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}
	.product-small {
		flex: 1;
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 1.5rem;
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: 16px;
		text-decoration: none;
		color: var(--text);
		transition: all 0.3s ease;
	}
	.product-small:hover {
		border-color: var(--border);
		background: var(--bg-elevated);
		transform: translateX(4px);
	}
	.product-icon-small { font-size: 2rem; }
	.product-small h4 { font-size: 1rem; font-weight: 600; margin-bottom: 0.25rem; }
	.product-small p { font-size: 0.8125rem; color: var(--text-secondary); }
	.product-small .arrow {
		margin-left: auto;
		font-size: 1.25rem;
		color: var(--text-muted);
		transition: transform 0.3s ease;
	}
	.product-small:hover .arrow { transform: translateX(4px); color: var(--accent); }

	/* Posts */
	.posts-section {
		padding: 6rem 0 8rem;
	}
	.posts-header {
		display: flex;
		justify-content: space-between;
		align-items: flex-end;
	}
	.view-all {
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.875rem;
		color: var(--accent);
		text-decoration: none;
	}
	.view-all:hover { text-decoration: underline; }

	.posts-grid {
		display: grid;
		grid-template-columns: 1.5fr 1fr;
		gap: 2rem;
	}

	.post-featured {
		display: grid;
		grid-template-rows: 1fr auto;
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: 24px;
		overflow: hidden;
		text-decoration: none;
		color: var(--text);
		transition: all 0.4s ease;
	}
	.post-featured:hover {
		border-color: rgba(99, 102, 241, 0.3);
		transform: translateY(-4px);
	}
	.post-image-featured {
		aspect-ratio: 16/9;
		background: var(--bg-elevated);
		overflow: hidden;
	}
	.post-image-featured img {
		width: 100%; height: 100%;
		object-fit: cover;
		transition: transform 0.5s ease;
	}
	.post-featured:hover .post-image-featured img { transform: scale(1.05); }
	.post-content-featured {
		padding: 2rem;
	}
	.post-tag {
		display: inline-block;
		padding: 0.25rem 0.75rem;
		background: rgba(99, 102, 241, 0.1);
		color: var(--accent);
		font-size: 0.6875rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		border-radius: 100px;
		margin-bottom: 1rem;
	}
	.post-content-featured h3 {
		font-size: 1.5rem;
		font-weight: 700;
		margin-bottom: 0.75rem;
		line-height: 1.3;
	}
	.post-content-featured p {
		color: var(--text-secondary);
		font-size: 0.9375rem;
		line-height: 1.6;
		margin-bottom: 1rem;
	}
	.post-date {
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.75rem;
		color: var(--text-muted);
	}

	.posts-list {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}
	.post-item {
		display: flex;
		align-items: center;
		gap: 1.25rem;
		padding: 1.5rem;
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: 16px;
		text-decoration: none;
		color: var(--text);
		transition: all 0.3s ease;
	}
	.post-item:hover {
		background: var(--bg-elevated);
		transform: translateX(4px);
	}
	.post-number {
		font-family: 'JetBrains Mono', monospace;
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--text-muted);
	}
	.post-info { flex: 1; }
	.post-info h4 {
		font-size: 1rem;
		font-weight: 600;
		margin-bottom: 0.25rem;
	}
	.post-arrow {
		font-size: 1.25rem;
		color: var(--text-muted);
		transition: all 0.3s ease;
	}
	.post-item:hover .post-arrow { transform: translateX(4px); color: var(--accent); }

	/* Responsive */
	@media (max-width: 1100px) {
		.hero { flex-direction: column; text-align: center; }
		.hero-container { max-width: 100%; }
		.hero-stats { justify-content: center; }
		.hero-cta { justify-content: center; }
		.floating-cards { display: none; }
		.products-showcase { grid-template-columns: 1fr; }
		.posts-grid { grid-template-columns: 1fr; }
	}
	@media (max-width: 768px) {
		main { padding: 0 1.5rem; }
		.hero-title .line { font-size: 2.5rem; }
		.products, .posts-section { padding: 4rem 0; }
	}
</style>
